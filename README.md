#Cable My Street Mock API
Basic Flask app to mock API services for Cable My Street web application

### Install the requirements
pip install -r requirements.txt

### Create DB
./manage.py createdb

### Run the server
./manage.py server

### Run tests
py.test tests